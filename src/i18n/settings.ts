export const FALLBACK_LOCALE = 'en'

export const languagesFallback = {
    en: ['en-us', 'en-gb'],
    'pt-BR': ['pt-br', 'pt-pt', 'pt'],
}
export const langKeys = Object.keys(languagesFallback)
export const langEntries = Object.entries(languagesFallback)

export const languages = {
    en: 'English',
    'pt-BR': 'Português (Brasil)',
}
